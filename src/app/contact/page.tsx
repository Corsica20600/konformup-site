import { Page, Breadcrumb } from "@/components/site-shell"; import { InquiryForm } from "@/components/forms"; import { site } from "@/lib/site-config"; export const metadata={title:"Contact"}; export default function Contact(){return <Page><section className="page-hero"><Breadcrumb current="Contact"/><p className="eyebrow gold">Échangeons</p><h1>Une question ? Écrivez-nous.</h1>{site.contact.email?<p className="lead">Vous pouvez aussi nous écrire à <a href={`mailto:${site.contact.email}`}>{site.contact.email}</a>.</p>:null}</section><section className="section form-wrap"><InquiryForm kind="contact"/></section></Page>}
