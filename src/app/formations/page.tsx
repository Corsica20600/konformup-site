import Link from "next/link"; import { Page, Breadcrumb } from "@/components/site-shell"; import { trainings } from "@/lib/site-config"; export const metadata={title:"Formations"}; export default function Formations(){return <Page><section className="page-hero"><Breadcrumb current="Formations"/><p className="eyebrow gold">Notre catalogue</p><h1>Former pour prévenir, décider et faire évoluer les pratiques.</h1><p className="lead">Des parcours à organiser selon votre activité, vos équipes et vos contraintes.</p></section><section className="section"><div className="offer-grid">{trainings.map((t,n)=><Link key={t.slug} href={`/formations/${t.slug}`} className="offer"><span>0{n+1}</span><h2>{t.title}</h2><p>{t.summary}</p><b>Voir le programme →</b></Link>)}</div></section></Page>}
