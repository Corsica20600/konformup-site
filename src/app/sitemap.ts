import type { MetadataRoute } from "next"; import { trainings, site } from "@/lib/site-config"; export default function sitemap():MetadataRoute.Sitemap{return ["","/formations","/methode-pedagogique","/avis","/a-propos","/faq","/demande-devis","/contact","/mentions-legales","/politique-confidentialite",...trainings.map(t=>`/formations/${t.slug}`)].map(url=>({url:`${site.url}${url}`,lastModified:new Date(),changeFrequency:"monthly",priority:url===""?1:0.7}))}
